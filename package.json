{
  "name": "covid-vaccination-stats-backend",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "node server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
